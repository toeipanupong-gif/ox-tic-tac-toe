/** จำกัด scan ใน memory ตอน substring search / sort ตาม PII (ห้าม findMany ไม่มีขอบเขต) */
export const ADMIN_IN_MEMORY_SCAN_LIMIT = 500;

/** จำกัดจำนวนแถวตอน export Excel */
export const ADMIN_EXPORT_ROW_LIMIT = 5_000;
